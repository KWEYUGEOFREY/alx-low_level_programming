#include <stdlib.h>

#include <stdio.h>
/**
 * main - Determine if a random number is positive, negative or zero.
(*
 * Return: 0 on success
 */
int main(void)
{

char smallletter = 'z';

 
 while(smallletter >= 'a')
 {
   putchar(smallletter);
    smallletter--;
 }
 putchar('\n');
	return(0);

}
